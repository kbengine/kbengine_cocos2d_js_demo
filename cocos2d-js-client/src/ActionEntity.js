var ActionEntity = ActionSprite.extend({
    ctor:function (scene, res) {
        //////////////////////////////
        // super init first
        this._super(scene, res);
        return true;
    }
});