var Avatar = ActionEntity.extend(
{
    ctor:function (scene, res) {
        //////////////////////////////
        // super init first
        this._super(scene, res);
        return true;
    },
    	
    motionUpdate : function(dt)
    {
    }  	
});